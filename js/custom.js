//1. function define one time process
function myCar(){ //formal argument/parameter
    //function body
    //statements
    //variable define inside a function is a local variable
    let car = "Verna"
    // console.log(car); //local variable access only inside the function
}
//2.function calling/invoking is a many time process
myCar();
console.log(car);